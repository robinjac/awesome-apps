{
    "name": "daily-site",
    "version": "0.0.0",
    "license": "MIT",
    "scripts": {
        "dev:update-daily": "yarn tsc --project tsconfig.update-daily.json --watch --outDir dist",
        "build:update-daily": "yarn tsc --project tsconfig.update-daily.json --outDir ../../.scripts",
        "dev": "vite",
        "build": "tsc && vite build",
        "serve": "vite preview",
        "test": "echo test"
    },
    "devDependencies": {
        "@types/node": "^16.0.0",
        "autoprefixer": "^10.3.1",
        "esbuild": "^0.12.20",
        "postcss": "^8.3.6",
        "postcss-import": "^14.0.2",
        "tailwindcss": "^2.2.7",
        "typescript": "^4.3.2",
        "vite": "^2.4.4",
        "vite-plugin-elm": "^2.4.2"
    }
}
